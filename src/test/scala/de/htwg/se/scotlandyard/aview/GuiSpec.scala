package de.htwg.se.scotlandyard.aview

import de.htwg.se.scotlandyard.controllerComponent.controllerBaseImpl.Controller
import de.htwg.se.scotlandyard.model.GameMaster
import org.scalatest.{Matchers, WordSpec}

class GuiSpec extends WordSpec with Matchers {
    "Gui" when {
      /*"created" should {
        GameMaster.initialize()
        val gui = new Gui(new Controller())
        "should updateGame()" in {
          gui.updateGame()
        }
        "should updateSettings()" in {
          gui.updateSettings()
        }
        "should changeToGamePanel()" in {
          gui.changeToGamePanel()
        }
        "should showWinningDialog()" in {
          gui.showWinningDialog()
        }
      }*/
    }
}
